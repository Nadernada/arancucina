import { Block } from 'payload'

export const WhoWeAreBlock: Block = {
  slug: 'WhoWeAreBlock',
  interfaceName: 'Who We Are',
  fields: [
    {
      name: 'title',
      type: 'text',
    },
  ],
}
